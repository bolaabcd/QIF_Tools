// Might change gang
uni <- uniform[0..5]; // Change to 0..2 for lesse dense examples
g <- ((uni) [NUMCHANCEUNI/DENOMCHANCEUNI] (g));
y = b + (4*g); 
// Changing building according to gang strategy (gang acts as soon as it receives the stash, adversary can't notice that the building didn't change as she could do if the stash wasn't moved when exchanged between gangs)
b = l[y];
y = b + (4*g);

